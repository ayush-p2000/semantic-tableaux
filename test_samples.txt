test_formulas = [
    # "<>(p | ~p)",
    # "[](p -> p)",
    # "<>p -> []<>p",
    # "[]p -> p",  # Valid (T axiom)
    # "[]p -> [][]p",
    # "<>[]p -> []<>p",
    # "[](<>p -> q) -> (<>[]p -> []q)",
    # "<>(p & q) -> (<>p & <>q)",
    # "[]p -> <>p",
    # "<>[]p -> []p",
    # "[](p | q) -> ([]p | []q)",
    # "[]p -> []<>p",
    # "<>[]p -> []p",
    # "[]<>[]p -> []p",
    # "([]p & []q) -> [](p & q)",
    # "<>(p -> q) -> ([]p -> <>q)",
    # "[]p -> (q -> []q)",
    # "<>(p & ~p)",
    # "<>p",
    # "(p | ~p)",
    # "(p & ~q)|(~p&r)",
    # "(p|q)->r",
    # "p & (p -> ~p)",
    # "(p&~q)&(q->~p)",
    # "p&~p",
    # "[](p | q) -> ([]p | <>q)"
    # "[][][][][]p"
    # "(p -> (q | z))"
    # "<>(p | q) -> (<>p | <>q)"
    # "<>p -> []p"
    # "p -> (p | q)"
    # "p & ~p"
    # "~(P & Q) | R"
    # "<>p -> []<>p"
    # "[]p -> [](p | q)"
    # "[]p -> r & <>q | ~s"
    # "<>p -> ~[]~p"
]